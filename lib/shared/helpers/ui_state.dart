enum UIState { success, loading, empty, error, initial }

enum ActivityState {nebengOnly, nitipOnly, nebengnitip, none}

enum UploadPictState { empty, fromLocal, fromNetwork }

enum PriceState {success, loading, initial}